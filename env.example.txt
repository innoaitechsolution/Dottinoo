# Supabase Configuration
# Get these values from your Supabase project dashboard: https://app.supabase.com

NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Service Role Key (server-side only, never expose to client)
# Get this from Supabase project dashboard: Settings → API → service_role key
# WARNING: This key has admin privileges. Only use in server-side API routes.
SUPABASE_SERVICE_ROLE_KEY=

# Demo Seed Feature
# Set to "true" to enable demo seed feature for teachers/admins
# In production, this should be "false"
# The API route will check this value and return 403 if not enabled
DEMO_SEED_ENABLED=true

